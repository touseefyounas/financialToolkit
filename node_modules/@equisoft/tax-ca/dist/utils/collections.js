"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.maxBy = maxBy;
function maxBy(values, mapFn) {
    const max = Math.max(...values.map(mapFn));
    return values.find((item) => mapFn(item) === max);
}
